---
- name: configure / hostname setting
  expect:
    replace: dest=/etc/hostname
      regexp='^ip-.*'
      replace="{{ hostname }}"
      backup=yes
    responses:
      'Please input hostname': "{{ hostname }}"
...
