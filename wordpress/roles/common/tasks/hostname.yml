---
- name: configure / input hostname
  pause:
    prompt: Please input hostname
  register: input_hostname

- name: configure / hostname settings
  lineinfile:
    path: /etc/hostname
    regexp: '^ip-.*'
    insertafter: ' {{ input_hostname }} '
    state: present
    line: '^ip-.*'
    backup: yes
...
